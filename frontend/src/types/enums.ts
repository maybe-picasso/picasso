export enum SocketMessageType {
  CHAT = 'CHAT',
  DRAWING = 'DRAWING',
}

export enum DrawingTools {
  PEN = 'PEN',
  ERASER = 'ERASER',
  CLEAR_ALL = 'CLEAR_ALL',
}

export enum DrawingStatusType {
  START = 'START',
  DRAW = 'DRAW',
  END = 'END',
}
